# CodeQL 静态分析（二阶段 P2 security gate）。
#
# JavaScript/TypeScript 全仓库扫描，外加 secret 检测查询集；push 到 main 与 PR
# 都会运行。权限最小化：只读 security-events 写入。
name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # 每周一 UTC 03:20 的例行扫描，覆盖无提交周的安全规则更新。
    - cron: '20 3 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

concurrency:
  group: codeql-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      # Pin the commit behind the moving `v4` tag (v4.38.0). The previous SHA did not
      # exist in github/codeql-action, so this whole security gate failed to resolve
      # before a single line was analysed.
      - uses: github/codeql-action/init@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4
        with:
          languages: javascript-typescript
          queries: security-extended

      - uses: github/codeql-action/analyze@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4
        with:
          category: '/language:javascript-typescript'
